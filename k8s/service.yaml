apiVersion: v1
kind: Service
metadata:
  name: rs-inventory-app-service
spec:
  type: NodePort
  selector:
    app: rs-inventory-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32002 # Standardized NodePort mapped to Docker Compose Port Number.